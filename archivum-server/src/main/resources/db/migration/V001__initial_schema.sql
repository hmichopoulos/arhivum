-- Initial schema for Archivum
-- This is a placeholder that will be expanded with actual tables

-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create initial version tracking table (Flyway will create its own, but this is for reference)
-- The actual schema will be added in subsequent migrations
